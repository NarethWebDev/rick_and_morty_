import 'package:flutter/material.dart';

class AppColors {
  // ── Portal / Verde neón (contorno del logo) ──────────────────────────────
  static const Color portalGreen     = Color(0xFF7CFC00);
  static const Color portalGreenDark = Color(0xFF44A000);
  static const Color portalGlow      = Color(0xFFAAFF44);

  // ── Azul Rick (relleno del logo) ─────────────────────────────────────────
  static const Color rickBlue        = Color(0xFF4DC3E8);
  static const Color rickBlueDark    = Color(0xFF2A9BBF);
  static const Color rickBlueDeep    = Color(0xFF1A6B8A);

  // ── Fondos espaciales ────────────────────────────────────────────────────
  static const Color spaceDark       = Color(0xFF060B14);
  static const Color spaceDeep       = Color(0xFF0A1020);
  static const Color spaceCard       = Color(0xFF0D1B2A);
  static const Color spaceSurface    = Color(0xFF111D2E);

  // ── Morty amarillo ───────────────────────────────────────────────────────
  static const Color mortyYellow     = Color(0xFFF7C948);
  static const Color mortyYellowDark = Color(0xFFD4A017);

  // ── Texto ────────────────────────────────────────────────────────────────
  static const Color textPrimary     = Color(0xFFECF0F1);
  static const Color textSecondary   = Color(0xFF8BA3BC);
  static const Color textMuted       = Color(0xFF3D5A80);

  // ── Navbar inferior ──────────────────────────────────────────────────────
  static const Color navbarBg        = Color(0xFF07111F);
  static const Color navbarBorder    = Color(0xFF1A3050);
  static const Color navbarActive    = portalGreen;
  static const Color navbarInactive  = Color(0xFF2E4A66);

  // ── AppBar ───────────────────────────────────────────────────────────────
  static const Color appBarBg        = Color(0xFF040A12);
  static const Color appBarBorder    = Color(0xFF7CFC00);

  // ── Alias legacy ─────────────────────────────────────────────────────────
  static const Color labBlue         = rickBlue;
}